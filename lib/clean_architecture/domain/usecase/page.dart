enum LoadPage { loading, normal }

enum Session { active, inactive }

enum Account { active, inactive }

enum ShoppingCart { active, inactive }

enum Home { active, inactive }

enum TypeFilter {category, keyword, search}

enum CardType {
  otherBrand,
  mastercard,
  visa,
  americanExpress,
  discover,
}

enum ManagerTypePhotoViewer { single, navigation }

enum LoadStatus {
  normal,
  error,
  loading,
  completed,
}